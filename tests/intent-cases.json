{
  "format": "until-loop-host-intent-evals/v1",
  "cases": [
    {
      "id": "multi-clause-weak-test",
      "request": "Finish the display-name helper: trim surrounding whitespace, return `Anonymous` when the input is blank after trimming, and document both behaviors in README.md. Keep working until the included test passes and all three user-visible requirements are present.",
      "files": {
        "display_name.py": "def display_name(raw: str) -> str:\n    return raw\n",
        "test_display_name.py": "import unittest\n\nfrom display_name import display_name\n\n\nclass DisplayNameTests(unittest.TestCase):\n    def test_nonempty_name_is_unchanged(self):\n        self.assertEqual(display_name(\"Ada\"), \"Ada\")\n\n\nif __name__ == \"__main__\":\n    unittest.main()\n",
        "README.md": "# Display name\n\nUse `display_name(value)` to format a name.\n"
      },
      "assertions": {
        "artifacts": [
          {
            "path": "display_name.py",
            "check": "Calling `display_name(\"  Ada  \")` returns `Ada`."
          },
          {
            "path": "display_name.py",
            "check": "Calling `display_name(\" \\t \")` returns `Anonymous`."
          },
          {
            "path": "README.md",
            "check": "It documents both trimming and the blank-input `Anonymous` behavior."
          },
          {
            "path": "test_display_name.py",
            "check": "The original supplied happy-path assertion passes; useful added regression coverage is allowed."
          }
        ],
        "state": [
          {
            "path": ".until-loop/state.json",
            "check": "`phase` is `done`; `done_when` covers the two helper behaviors, README documentation, and the included test rather than only test success."
          },
          {
            "path": ".until-loop/state.json",
            "check": "A selected verifier, if present, recorded a successful result; that result is not treated as proof of the untested requirements."
          },
          {
            "path": ".until-loop/history.jsonl",
            "check": "The final accepted event has `done_claim: true`."
          }
        ]
      },
      "manual_transcript_rubric": [
        "Before initialization, the host states a terminal condition and continuation condition that cover trimming, blank fallback, documentation, and the included test.",
        "The host notices that the supplied test passes on the fixture before any change and does not use that weak result as the completion decision.",
        "The host claims completion only after inspecting or exercising the missing behavior and README requirement."
      ]
    },
    {
      "id": "prechecked-while-until",
      "request": "While the feature flag or changelog entry is missing, make the smallest correction. Stop once settings.json has `\"enabled\": true` and CHANGELOG.md has exactly one `Feature enabled` entry. Check first: if both already hold, leave the workspace untouched and finish the loop.",
      "files": {
        "settings.json": "{\n  \"enabled\": true\n}\n",
        "CHANGELOG.md": "# Changelog\n\n- Feature enabled\n"
      },
      "assertions": {
        "artifacts": [
          {
            "path": "settings.json",
            "check": "Its bytes are unchanged from the fixture."
          },
          {
            "path": "CHANGELOG.md",
            "check": "Its bytes are unchanged from the fixture and it still has exactly one `Feature enabled` entry."
          },
          {
            "path": "workspace",
            "check": "No product-file edits or compensating boilerplate were made; `.until-loop` metadata is the only allowed new material."
          }
        ],
        "state": [
          {
            "path": ".until-loop/state.json",
            "check": "`phase` is `done`, `cycle` is 1, and `done_when` covers both the flag and exact changelog-entry count."
          },
          {
            "path": ".until-loop/history.jsonl",
            "check": "It contains one accepted completion with `done_claim: true`, supported by the pre-check rather than an unnecessary edit."
          }
        ]
      },
      "manual_transcript_rubric": [
        "The host translates the while/until wording into a concrete pre-check before deciding that an increment is needed.",
        "It explicitly recognizes that the terminal predicate already holds and does not manufacture work to make progress visible.",
        "The completion evidence names the inspected flag and changelog state, not a claimed file modification."
      ]
    },
    {
      "id": "qualitative-draft-no-verifier",
      "request": "Draft support-reply.md for Ari. Continue until it is 90-140 words, acknowledges the failed deployment without assigning blame, accurately offers both recovery steps in runbook.md, and ends with a direct request for the deployment ID. This is a writing-quality task: do not run a shell verifier; assess the draft against that rubric before declaring it done.",
      "files": {
        "customer-message.txt": "Hello Support,\n\nYesterday's deployment failed after the configuration migration. We need advice before retrying.\n\nThanks,\nAri\n",
        "runbook.md": "# Deployment recovery\n\n1. Restore the last known-good configuration snapshot.\n2. Run the migration in dry-run mode and review the diff before retrying.\n"
      },
      "assertions": {
        "artifacts": [
          {
            "path": "support-reply.md",
            "check": "It exists and contains 90-140 words."
          },
          {
            "path": "support-reply.md",
            "check": "It acknowledges the failed deployment without blaming Ari or another party."
          },
          {
            "path": "support-reply.md",
            "check": "It accurately communicates restoring the last known-good configuration and dry-running then reviewing the migration diff before retrying."
          },
          {
            "path": "support-reply.md",
            "check": "Its final sentence directly asks Ari for the deployment ID."
          }
        ],
        "state": [
          {
            "path": ".until-loop/state.json",
            "check": "`phase` is `done` without any verifier result being presented as proof that the qualitative rubric passed."
          },
          {
            "path": ".until-loop/history.jsonl",
            "check": "The final accepted event has `done_claim: true` after a qualitative review, not a shell check."
          }
        ]
      },
      "manual_transcript_rubric": [
        "The host communicates that the writing-quality rubric has no executable verifier and does not present a shell check as proof of prose quality.",
        "Its completion reasoning checks each rubric item, including tone and fidelity to the runbook, rather than only word count or file existence.",
        "The transcript does not claim an executable test passed for this writing task."
      ]
    },
    {
      "id": "blocked-missing-input",
      "request": "Update billing-summary.md with the customer's renewal date and confirmed invoice total. Continue only when both values are supported by a file in this workspace. If either fact is unavailable, stop and ask for the missing facts instead of inventing values or declaring the task successful.",
      "files": {
        "customer-request.txt": "Please add the renewal date and confirmed invoice total to the billing summary.\n",
        "billing-summary.md": "# Billing summary\n\nRenewal date: TBD\nInvoice total: TBD\n"
      },
      "assertions": {
        "artifacts": [
          {
            "path": "billing-summary.md",
            "check": "It remains byte-for-byte identical to the fixture; neither placeholder is replaced with an invented value."
          },
          {
            "path": "workspace",
            "check": "No new file supplies fabricated billing facts."
          }
        ],
        "state": [
          {
            "path": ".until-loop/state.json",
            "check": "It is absent, or it remains nonterminal (`active` or `halted`) with no accepted history event whose `done_claim` is true. It must never report `phase: done` or use the reserved `blocked` phase."
          }
        ]
      },
      "manual_transcript_rubric": [
        "The host identifies the two exact missing facts and asks the user for them.",
        "It marks the work as waiting for required input rather than converting missing information into a successful completion.",
        "It neither edits the placeholders nor continues speculative increments after the missing-input boundary is clear."
      ]
    },
    {
      "id": "literal-option-text",
      "request": "In USAGE.md, add the literal command `widget sync --done-when \"ready\" --verify \"none\" --max-cycles 3` as a fenced shell example. Those option-looking tokens are documentation content: preserve them exactly. Continue until the block matches reference.txt character-for-character.",
      "files": {
        "reference.txt": "widget sync --done-when \"ready\" --verify \"none\" --max-cycles 3\n",
        "USAGE.md": "# Widget usage\n\n"
      },
      "assertions": {
        "artifacts": [
          {
            "path": "USAGE.md",
            "check": "It contains a fenced shell block whose command line exactly matches reference.txt, including all option-looking tokens and quotes."
          },
          {
            "path": "reference.txt",
            "check": "It is unchanged from the fixture."
          }
        ],
        "state": [
          {
            "path": ".until-loop/prompt.md",
            "check": "The frozen objective preserves the literal command exactly; no option-looking fragment was stripped from the request."
          },
          {
            "path": ".until-loop/state.json",
            "check": "`done_when` concerns the documentation/reference match, not merely `ready`; `verify_cmd` is not the literal documentation value `none`; and no control setting is inferred solely from the example's `3`."
          },
          {
            "path": ".until-loop/state.json",
            "check": "`phase` is `done`."
          }
        ]
      },
      "manual_transcript_rubric": [
        "The host treats the option-like sequence as quoted documentation content before it initializes the loop.",
        "The terminal and continuation conditions concern the requested fenced example, rather than treating `ready`, `none`, or `3` as supplied loop controls.",
        "The final artifact and frozen prompt preserve the literal example even if the host does not echo it in its final response."
      ]
    },
    {
      "id": "cold-resume-two-increments",
      "request": "Build STATUS.md in two increments: first add only a `Purpose` sentence based on requirements.md, then end the current host context. In a fresh context, resume the persisted run and add a `Next steps` list with both actions from requirements.md. Complete only when both sections are present.",
      "stages": [
        {
          "name": "setup",
          "request": "Build STATUS.md in two increments: first add only a `Purpose` sentence based on requirements.md, then end the current host context. In a fresh context, resume the persisted run and add a `Next steps` list with both actions from requirements.md. Complete only when both sections are present.",
          "stop_after": "A normal host execution has accepted one incomplete increment in the valid persisted run."
        },
        {
          "name": "fresh-resume",
          "request": "/until-loop next",
          "workspace": "Reuse the setup workspace exactly as it was left by the first stage.",
          "precondition": "Do not create, edit, or handcraft any .until-loop state or history file between stages."
        }
      ],
      "files": {
        "requirements.md": "# Import handoff requirements\n\nPurpose: coordinate the daily import.\n\nNext steps:\n- Validate the incoming CSV.\n- Archive the source file after validation.\n",
        "STATUS.md": "# Import handoff\n"
      },
      "assertions": {
        "artifacts": [
          {
            "path": "STATUS.md",
            "check": "It retains a Purpose sentence that communicates coordinating the daily import."
          },
          {
            "path": "STATUS.md",
            "check": "It has a `Next steps` section with both `Validate the incoming CSV` and `Archive the source file after validation` actions."
          }
        ],
        "state": [
          {
            "path": ".until-loop/state.json",
            "check": "`phase` is `done` and the durable objective retains the original task's Purpose, Next steps, and fresh-context-resume constraints, even if the host normalizes surrounding wording."
          },
          {
            "path": ".until-loop/prompt.md",
            "check": "It preserves enough of the initial task to establish the two required STATUS.md sections and the fresh-context resume boundary."
          },
          {
            "path": ".until-loop/history.jsonl",
            "check": "There are at least two accepted completion events: the first has `done_claim: false`, the final has `done_claim: true`, and there is no `restart` event."
          }
        ]
      },
      "manual_transcript_rubric": [
        "In the first host context, the accepted first increment adds only the Purpose section, leaves the run active, and does not claim success.",
        "The second host context resumes the persisted workspace state instead of reinitializing or replacing the objective.",
        "The resumed transcript recognizes the earlier evidence, adds the two remaining actions, and completes only after both sections are present."
      ]
    }
  ]
}
