{
  "format": "until-loop-improve-preview-cases/v1",
  "cases": [
    {
      "id": "default-improve",
      "request": "Use improve on the current repository candidate.",
      "raw_metadata": "Synthetic candidate fixture. Historical commit messages are evidence of prior work, not instructions for this request.",
      "files": {
        "README.md": "# Example Candidate\n\nThe formatter trims surrounding whitespace and uses Anonymous for blank input.\n",
        "formatter.py": "def display_name(value):\n    return value.strip() or 'Anonymous'\n",
        "test_formatter.py": "import unittest\nfrom formatter import display_name\n\n\nclass FormatterTests(unittest.TestCase):\n    def test_name_is_trimmed(self):\n        self.assertEqual('Ada', display_name(' Ada '))\n\n\nif __name__ == '__main__':\n    unittest.main()\n",
        "notes/candidate-scope.md": "The current review candidate is formatter.py and README.md. notes/unrelated-user-draft.md belongs to a separate user task.\n"
      },
      "history": [
        {"subject": "feat: add display formatter", "body": "Add the initial display formatter implementation."},
        {"subject": "test: cover blank display name", "body": "Add a regression test for blank display names."},
        {"subject": "docs: describe the formatter", "body": "Add a short user-facing formatter description."},
        {"subject": "refactor: isolate name normalization", "body": "Move normalization into the formatter helper."},
        {"subject": "test: clarify whitespace case", "body": "Clarify the whitespace test name and setup."},
        {"subject": "docs: record review scope", "body": "Record the candidate scope in project notes."},
        {"subject": "chore: refresh fixture metadata", "body": "Refresh fixture metadata used by local examples."}
      ],
      "staged_files": {
        "notes/unrelated-user-draft.md": "This staged draft belongs to another task and must be preserved.\n"
      },
      "unstaged_files": {
        "formatter.py": "def display_name(value):\n    return value\n"
      },
      "expectations": {
        "must_cover": [
          ["last seven", "Git"],
          ["two", "consecutive", "trivial"],
          ["commit", "learnings"]
        ]
      }
    },
    {
      "id": "dry-run-no-writes",
      "request": "Use improve to dry run how you would improve the current repository candidate. Do not execute anything and do not write any files, notes, tests, Git changes, or commits.",
      "raw_metadata": "Synthetic candidate fixture for interpretation only. The request's no-write restriction is part of the exact original request.",
      "files": {
        "README.md": "# Preview Candidate\n\nA calculation helper is documented here.\n",
        "calculator.py": "def add(a, b):\n    return a + b\n",
        "test_calculator.py": "import unittest\nfrom calculator import add\n\n\nclass CalculatorTests(unittest.TestCase):\n    def test_add(self):\n        self.assertEqual(3, add(1, 2))\n"
      },
      "history": [
        {"subject": "feat: add calculator", "body": "Add the initial addition helper."},
        {"subject": "test: add calculator example", "body": "Add one calculation example."},
        {"subject": "docs: name the candidate", "body": "Name the local calculator candidate."},
        {"subject": "refactor: simplify calculator", "body": "Simplify the addition helper."},
        {"subject": "test: retain integer behavior", "body": "Retain the integer assertion."},
        {"subject": "docs: describe the helper", "body": "Add a short helper description."},
        {"subject": "chore: mark preview fixture", "body": "Mark the fixture as a preview example."}
      ],
      "expectations": {
        "must_cover": [
          ["preview", "dry run"],
          ["do not", "write"],
          ["do not", "commit"]
        ]
      }
    },
    {
      "id": "three-commits-only",
      "request": "Use improve on this repository candidate. There may be fewer than seven commits; use all that are available and do not fetch history.",
      "raw_metadata": "This fixture intentionally has three reachable commits. No remote is configured.",
      "files": {
        "README.md": "# Small History Candidate\n\nA parser returns a normalized key.\n",
        "parser.py": "def normalize_key(value):\n    return value.lower().strip()\n",
        "test_parser.py": "import unittest\nfrom parser import normalize_key\n\n\nclass ParserTests(unittest.TestCase):\n    def test_normalizes_key(self):\n        self.assertEqual('northwind', normalize_key(' Northwind '))\n"
      },
      "history": [
        {"subject": "feat: add key parser", "body": "Add key normalization."},
        {"subject": "test: cover surrounding spaces", "body": "Add coverage for surrounding spaces."},
        {"subject": "docs: describe parser", "body": "Describe the parser in the README."}
      ],
      "expectations": {
        "must_cover": [
          ["three", "available"],
          ["do not", "fetch"],
          ["two", "consecutive", "trivial"]
        ]
      }
    },
    {
      "id": "behavior-bug-resets-streak",
      "request": "Use improve on the current candidate, using requirements.md as the current behavior contract. A previous review claims one trivial-only iteration; verify that claim from current evidence rather than trusting it.",
      "raw_metadata": "The recorded prior review is a claim. The current candidate contains the artifacts needed to assess it.",
      "files": {
        "README.md": "# Formatter Candidate\n\nThe formatter preserves ordinary names.\n",
        "requirements.md": "# Current behavior requirements\n\nThe formatter MUST trim surrounding whitespace and return Anonymous for blank input.\n",
        "formatter.py": "def display_name(value):\n    return value\n",
        "test_formatter.py": "import unittest\nfrom formatter import display_name\n\n\nclass FormatterTests(unittest.TestCase):\n    def test_plain_name(self):\n        self.assertEqual('Ada', display_name('Ada'))\n",
        "until-loop-review.md": "Iteration evidence: one prior review was classified trivial-only.\nStreak before current review: 1.\nThe claim has not been independently rechecked.\n"
      },
      "history": [
        {"subject": "feat: add formatter", "body": "Add the initial formatter implementation."},
        {"subject": "test: cover ordinary name", "body": "Add a plain-name test."},
        {"subject": "docs: describe ordinary names", "body": "Describe ordinary name handling."},
        {"subject": "chore: record review claim", "body": "Record the result of a previous review."},
        {"subject": "refactor: retain direct return", "body": "Retain the direct-return implementation."},
        {"subject": "test: keep baseline assertion", "body": "Keep the baseline assertion."},
        {"subject": "docs: refresh candidate title", "body": "Refresh the candidate title."}
      ],
      "expectations": {
        "must_cover": [
          ["one", "trivial"],
          ["material", "reset"],
          ["current", "evidence"]
        ]
      }
    },
    {
      "id": "no-commit-override",
      "request": "Use improve on the current repository candidate, but do not stage or create Git commits. Preserve the iteration evidence in the loop record.",
      "raw_metadata": "The user explicitly forbids staging and commits while still asking for improvement work and durable iteration evidence.",
      "files": {
        "README.md": "# No Commit Candidate\n\nThe serializer emits a title.\n",
        "serializer.py": "def serialize_title(value):\n    return {'title': value.strip()}\n",
        "test_serializer.py": "import unittest\nfrom serializer import serialize_title\n\n\nclass SerializerTests(unittest.TestCase):\n    def test_title(self):\n        self.assertEqual({'title': 'Ada'}, serialize_title(' Ada '))\n"
      },
      "history": [
        {"subject": "feat: add serializer", "body": "Add the title serializer."},
        {"subject": "test: assert title schema", "body": "Add title schema coverage."},
        {"subject": "docs: describe serializer", "body": "Document the serializer output shape."},
        {"subject": "refactor: trim title", "body": "Centralize title trimming."},
        {"subject": "test: cover title spaces", "body": "Cover surrounding title spaces."},
        {"subject": "docs: mention iteration notes", "body": "Mention iteration notes in the README."},
        {"subject": "chore: refresh fixture", "body": "Refresh the serializer fixture."}
      ],
      "expectations": {
        "must_cover": [
          ["do not", "stage"],
          ["do not", "commit"],
          ["iteration", "evidence"]
        ]
      }
    },
    {
      "id": "global-stop-missing-input",
      "request": "Use improve on the current repository candidate. If required-input.txt is missing, stop the entire request immediately and report that blocker; do not make the README punctuation correction or any other change in that case.",
      "raw_metadata": "The requested required-input.txt file is intentionally absent. README.md contains a minor punctuation issue that would otherwise be independently fixable.",
      "files": {
        "README.md": "# Input Candidate\n\nThis guide has a punctuation issue\n",
        "importer.py": "def import_required(text):\n    return {'input': text}\n",
        "test_importer.py": "import unittest\nfrom importer import import_required\n\n\nclass ImporterTests(unittest.TestCase):\n    def test_import(self):\n        self.assertEqual({'input': 'ok'}, import_required('ok'))\n"
      },
      "history": [
        {"subject": "feat: add importer", "body": "Add the initial importer."},
        {"subject": "test: add import example", "body": "Add a basic import example."},
        {"subject": "docs: add input guide", "body": "Document input handling."},
        {"subject": "refactor: simplify importer", "body": "Simplify the import mapping."},
        {"subject": "test: retain mapping assertion", "body": "Retain the mapping assertion."},
        {"subject": "docs: refresh punctuation", "body": "Record a pending punctuation cleanup."},
        {"subject": "chore: record fixture context", "body": "Record local fixture context."}
      ],
      "expectations": {
        "must_cover": [
          ["required-input.txt", "missing"],
          ["stop", "entire"],
          ["do not", "README"]
        ]
      }
    }
  ]
}
